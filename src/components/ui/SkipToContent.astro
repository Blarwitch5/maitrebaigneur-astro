<!-- btn go straight to content for accessibility -->
<a id="skip-to-content" class="sr-only" href="#skip-content-target">Aller au contenu</a>

<style>
	.skiplink,
	.skiplink:focus,
	.skiplink:focus-visible {
		position: absolute;
		padding: 0.25em;
		font-size: larger;
		top: 0;
		left: 0;
		right: 0;
		z-index: 9;
		display: block;
		text-align: center;
		background-color: var(--color-primary);
		color: var(--color-bg);
		border-radius: 0.25em;
		outline: var(--color-bg) solid 1px;
		outline-offset: 0;
	}
</style>
