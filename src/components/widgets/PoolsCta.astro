---
import Buttons from "@components/ui/Buttons.astro";
import Container from "@components/Container.astro";

import site from "@assets/data/data.json";

const { siteLinks } = site;
const pools = siteLinks.pools;
---

<Container className="pools__wrapper">
	<div class="pools__content">
		<h3 class="pools__title">Vous êtes à la recherche d'un bassin pour apprendre à nager ?</h3>
		<p class="pools__desc">
			Vous êtes au bon endroit. Nous opérons aussi bien à domicile que dans nos bassins d’apprentissage. Choisissez un de nos bassins et commencez
			votre aventure Maître-Baigneur. Nous sommes impatients de vous accueillir et de vous aider à atteindre vos objectifs.
		</p>
		<Buttons href="/bassins" class={`pools__btn btn btn__regular`}>Découvrir nos bassins</Buttons>
	</div>
	<div class="pools__images">
		<img
			loading="lazy"
			src="/images/pools/setclub.jpg"
			alt="Maître-Baigneur en action apprenant à nager à deux enfants avec du matériel pédagogique"
			class=""
			height="600"
			width="800"
		/>
	</div>
</Container>

<style lang="scss">
	.pools {
		&__wrapper {
			display: grid;
			grid-template-columns: calc((100% * 2 / 3) - 2rem) calc((100% * 1 / 3) - 2rem);
			gap: 4rem;
			align-items: center;
			padding-top: var(--space-lg);

			@media screen and (max-width: 992px) {
				grid-template-columns: calc(40% - 1rem) calc(60% - 1rem);
				gap: 2rem;
			}

			@media screen and (max-width: 768px) {
				grid-template-columns: 1fr;
				gap: 2rem;
			}
		}
		&__content {
			display: flex;
			flex-direction: column;
			gap: var(--space-md);
			& > * {
				margin-block: 0;
			}
		}
		&__images {
			overflow: hidden;
			border-radius: 20px;
			display: flex;
			align-items: center;
			justify-content: center;

			img {
				height: 100%;
				max-height: 370px;
				object-fit: cover;
			}
		}
	}
	.pool {
		max-width: 100px;
		overflow: hidden;
		&__img {
			width: 100%;
			height: 100%;
			object-fit: cover;
		}
	}
</style>
