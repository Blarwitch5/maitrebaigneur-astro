---
import { Picture } from 'astro:assets';
import Button from '@components/ui/Button.astro';
import Waves from '@components/ui/shapes/Waves.astro';
import Title from '@components/ui/Title.astro';

import joinus from '@assets/images/join-us-1.jpg';
---

<div class='container booking__section'>
  <div class='booking__img' data-aos='fade-right'>
    <Picture
      src={joinus}
      alt='Maître-Baigneur en action apprenant à nager à une petite fille avec du matériel pédagogique'
      width={600}
      height={800}
    />
  </div>
  <div class='booking__content' data-aos='zoom-in'>
    <Title level='4' class='booking__title'>
      Vous souhaitez commencer l’aventure avec Maître-Baigneur ?
    </Title>
    <p class='booking__txt'>
      Rejoignez-nous pour une expérience enrichissante, où notre équipe qualifiée vous accompagnera
      dans votre apprentissage de la natation, favorisant la confiance en soi et le bien-être
      aquatique. Quel que soit votre point de départ, nous sommes là pour vous aider à progresser et
      à apprécier les bienfaits de la natation à tout âge.
    </p>
    <Button class='btn btn__outline' href='https://clicrdv.com/maitrebaigneur' target='_blank'>
      Je réserve ma place dès maintenant !
    </Button>
  </div>
  <Waves atTopOfSection />
</div>

<style lang='scss'>
  :global(.booking__section) {
    display: grid;
    grid-template-columns: calc((100% * 1 / 3) - 2rem) calc((100% * 2 / 3) - 2rem);
    gap: 4rem;
    align-items: center;
    padding-top: var(--space-lg);

    @media screen and (max-width: 992px) {
      grid-template-columns: calc(40% - 1rem) calc(60% - 1rem);
      gap: 2rem;
    }

    @media screen and (max-width: 768px) {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }

  .booking__img {
    display: none;
    overflow: hidden;
    border-radius: var(--regular-border-radius);
    display: flex;
    align-items: center;
    justify-content: center;

    img {
      height: 100%;
      max-height: 370px;
      object-fit: contain;
    }
  }

  .booking__content {
    display: flex;
    gap: 1rem;
    flex-direction: column;
    color: var(--color-white);
  }

  .booking__title {
    color: inherit;
    margin-bottom: 0;
    font-size: var(--fs-lg);
    font-weight: bold;
  }
</style>
